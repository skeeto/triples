{
  "version": 3,
  "cmakeMinimumRequired": { "major": 3, "minor": 24, "patch": 0 },
  "configurePresets": [
    {
      "name": "mingw",
      "displayName": "mingw-w64 cross (Windows GCC, static)",
      "description": "Cross-compile a static Windows .exe from a Unix host with mingw-w64 installed (e.g. `brew install mingw-w64`). Useful for verifying the Windows-only build pieces (.rc + icon, VERSIONINFO, GUI subsystem, persistence_win.cpp) without leaving the host.",
      "binaryDir": "${sourceDir}/build-mingw",
      "toolchainFile": "${sourceDir}/cmake/mingw-w64.cmake",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release"
      }
    }
  ],
  "buildPresets": [
    {
      "name": "mingw",
      "configurePreset": "mingw"
    }
  ]
}
