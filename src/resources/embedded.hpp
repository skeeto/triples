#pragma once
// Declarations for blobs embedded at build time by cmake/EmbedResource.cmake.
// Each name corresponds to an embed_resource(name ...) call in CMakeLists.txt.

extern "C" {
extern const unsigned char  font_inter_bold_data[];
extern const unsigned long  font_inter_bold_size;
}
